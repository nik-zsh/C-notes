Logical operatos have sort of side effect in terms of thier operation

## && (AND):
If first statement is false second statement is never evaluated

## || (OR):
If first statement is true second statement is never executed