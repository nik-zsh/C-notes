In C langauge assignment is an operator rather than statememt

### Left side of assignment should be a Lvalue (pointing to memory related stuff)

Assignment Operator is **Right associative**
```c
i = j = k = 0;
```
is same as:
```c
i = (j = (k = 0));
```

